# Lingkungan eksperimen SawitMVC (torch sudah ada di python sistem: 2.8.0+cu128)
# Pasang ke venv terpisah setelah unduhan dataset selesai:
#   python3 -m venv /workspace/experiments/.venv
#   /workspace/experiments/.venv/bin/pip install -r requirements.txt

# Backend dav2 (Depth Anything V2, entri 175)
transformers>=4.45
pillow
numpy

# Deteksi + counting
ultralytics
opencv-python-headless
scikit-learn
pandas
pyarrow

# Backend da3mono / da3mv (Depth Anything 3, entri 198) TIDAK ada di PyPI.
# Paketnya harus dipasang dari sumber:
#   git clone https://github.com/ByteDance-Seed/depth-anything-3
#   /workspace/experiments/.venv/bin/pip install -e depth-anything-3
# Lisensi bobot DA3: CC BY-NC 4.0 (sejalan dengan lisensi SawitMVC).
