# Lingkungan eksperimen SawitMVC — versi PERSIS yang menghasilkan angka di
# docs/METRICS.md. Dikembangkan di NVIDIA L4, CUDA 12.8.
#
#   python3 -m venv /workspace/experiments/.venv
#   /workspace/experiments/.venv/bin/pip install -r requirements.txt

# torch/torchvision datang dari image sistem (bukan pip di sini):
#   torch==2.8.0+cu128   torchvision==0.23.0+cu128
# Pasang sesuai CUDA target dari https://pytorch.org bila lingkungan berbeda.

ultralytics==8.4.103
numpy==1.26.4
opencv-python-headless==4.11.0
pycocotools==2.0.11        # two_stage.py, metric_variants.py, eval_*_perkelas (mAP COCO)
scikit-learn               # class_separability.py (RandomForest, E-012)
pandas
pyarrow

# E-021 (RF-DETR vs RT-DETR vs YOLO26 — train_rfdetr.py, eval_all_pycoco/metrics,
# eval_extras, eval_efficiency). Default library rfdetr menentukan hyperparameter
# RF-DETR (lr, ema, warmup, dll) — versi HARUS dipin agar angka E-021 tereproduksi.
# Config efektif lengkap tiap run juga terekam di runs/<run>/training_config.json.
rfdetr==1.8.3
supervision==0.29.1        # dependensi rfdetr (Detections dari predict)
ultralytics-thop==2.0.20   # GFLOPs di eval_efficiency.py
matplotlib                 # figur PR/F1/confusion di eval_extras.py

# Backend Depth Anything V2 (entri 175) — gen_depth_dataset mode dav2
transformers>=4.45
pillow

# Backend Depth Anything 3 (entri 198) untuk E-003..E-007 / SR-003..SR-006.
# TIDAK di PyPI — pasang dari sumber (kode juga ada di experiments/da3-src/ pada
# workspace kerja, tidak diarsipkan ke repo karena besar):
#   git clone https://github.com/ByteDance-Seed/depth-anything-3
#   .venv/bin/pip install -e depth-anything-3
# Lisensi bobot DA3: CC BY-NC 4.0 (sejalan lisensi SawitMVC).
