---
source_id: 092
bibtex_key: ku2018avod
title: Joint 3D Proposal Generation and Object Detection from View Aggregation
year: 2018
domain_theme: Deteksi 3D
verified_pdf: 92_AVOD.pdf
char_count: 51068
---

Joint 3D Proposal Generation and Object Detection from View
                                                                         Aggregation
                                                         Jason Ku, Melissa Mozifian, Jungwook Lee, Ali Harakeh, and Steven L. Waslander

                                            Abstract— We present AVOD, an Aggregate View Object
                                         Detection network for autonomous driving scenarios. The
                                         proposed neural network architecture uses LIDAR point clouds                                                              θyaw
                                         and RGB images to generate features that are shared by
                                         two subnetworks: a region proposal network (RPN) and a
                                         second stage detector network. The proposed RPN uses a
arXiv:1712.02294v4 [cs.CV] 12 Jul 2018

                                         novel architecture capable of performing multimodal feature           θyaw
                                         fusion on high resolution feature maps to generate reliable
                                         3D object proposals for multiple object classes in road scenes.      Y                            Y
                                         Using these proposals, the second stage detection network
                                         performs accurate oriented 3D bounding box regression and
                                         category classification to predict the extents, orientation, and             X                           X
                                         classification of objects in 3D space. Our proposed architecture
                                         is shown to produce state of the art results on the KITTI 3D       Fig. 1: A visual representation of the 3D detection problem
                                         object detection benchmark [1] while running in real time with
                                         a low memory footprint, making it a suitable candidate for         from Bird’s Eye View (BEV). The bounding box in Green
                                         deployment on autonomous vehicles. Code is at:                     is used to determine the IoU overlap in the computation of
                                         https://github.com/kujason/avod                                    the average precision. The importance of explicit orientation
                                                                                                            estimation can be seen as an object’s bounding box does
                                                              I. I NTRODUCTION                              not change when the orientation (purple) is shifted by ±π
                                                                                                            radians.
                                            The remarkable progress made by deep neural networks
                                         on the task of 2D object detection in recent years has not            Region proposal networks (RPNs) were proposed in
                                         transferred well to the detection of objects in 3D. The gap        Faster-RCNN [2], and have become the prevailing proposal
                                         between the two remains large on standard benchmarks such          generators in 2D object detectors. RPNs can be considered a
                                         as the KITTI Object Detection Benchmark [1] where 2D car           weak amodal detector, providing proposals with high recall
                                         detectors have achieved over 90% Average Precision (AP),           and low precision. These deep architectures are attractive
                                         whereas the top scoring 3D car detector on the same scenes         as they are able to share computationally expensive con-
                                         only achieves 70% AP. The reason for such a gap stems              volutional feature extractors with other detection stages.
                                         from the difficulty induced by adding a third dimension            However, extending these RPNs to 3D is a non-trivial task.
                                         to the estimation problem, the low resolution of 3D input          The Faster R-CNN RPN architecture is tailored for dense,
                                         data, and the deterioration of its quality as a function of        high resolution image input, where objects usually occupy
                                         distance. Furthermore, unlike 2D object detection, the 3D          more than a couple of pixels in the feature map. When
                                         object detection task requires estimating oriented bounding        considering sparse and low resolution input such as the
                                         boxes (Fig. 1).                                                    Front View [3] or Bird’s Eye View (BEV) [4] point cloud
                                            Similar to 2D object detectors, most state-of-the-art deep      projections, this method is not guaranteed to have enough
                                         models for 3D object detection rely on a 3D region proposal        information to generate region proposals, especially for small
                                         generation step for 3D search space reduction. Using region        object classes.
                                         proposals allows the generation of high quality detections            In this paper, we aim to resolve these difficulties by
                                         via more complex and computationally expensive processing          proposing AVOD, an Aggregate View Object Detection
                                         at later detection stages. However, any missed instances at        architecture for autonomous driving (Fig. 2). The proposed
                                         the proposal generation stage cannot be recovered during the       architecture delivers the following contributions:
                                         following stages. Therefore, achieving a high recall during           • Inspired by feature pyramid networks (FPNs) [5] for 2D
                                         the region proposal generation stage is crucial for good                object detection, we propose a novel feature extractor
                                         performance.                                                            that produces high resolution feature maps from LIDAR
                                            1 Jason Ku, Melissa Mozifian, Jungwook Lee, Ali Harakeh, and
                                                                                                                 point clouds and RGB images, allowing for the local-
                                         Steven L. Waslander are with the Department of Mechanical and           ization of small classes in the scene.
                                         Mechatronics Engineering, Faculty of Engineering, University of       • We propose a feature fusion Region Proposal Network
                                         Waterloo, 200 University Avenue, Waterloo, ON, Canada. Email:           (RPN) that utilizes multiple modalities to produce high-
                                         (jason.ku@uwaterloo.ca, mfmozifi@uwaterloo.ca,
                                         j343lee@uwaterloo.ca, www.aharakeh.com,                                 recall region proposals for small classes.
                                         stevenw@uwaterloo.ca)                                                 • We propose a novel 3D bounding box encoding that
Fig. 2: The proposed method’s architectural diagram. The feature extractors are shown in blue, the region proposal network
in pink, and the second stage detection network in green.

     conforms to box geometric constraints, allowing for        better results when compared to both of these single shot
     higher 3D localization accuracy.                           methods. VoxelNet [9] extends 3D-FCN further by encoding
   • The proposed neural network architecture exploits 1 ×      voxels with point-wise features instead of occupancy values.
     1 convolutions at the RPN stage, along with a fixed        However, even with sparse 3D convolution operations,
     look-up table of 3D anchor projections, allowing high      VoxelNet’s computational speed is still 3× slower than our
     computational speed and a low memory footprint while       proposed architecture, which provides better results on the
     maintaining detection performance.                         car and pedestrian classes.
   The above contributions result in an architecture that
delivers state-of-the-art detection performance at a low com-   Monocular-Based Proposal Generation: Another direction
putational cost and memory footprint. Finally, we integrate     in the state-of-the-art is using mature 2D object detectors for
the network into our autonomous driving stack, and show         proposal generation in 2D, which are then extruded to 3D
generalization to new scenes and detection under more           through amodal extent regression. This trend started with
extreme weather and lighting conditions, making it a suitable   [10] for indoor object detection, which inspired Frustum-
candidate for deployment on autonomous vehicles.                based PointNets (F-PointNet) [11] to use point-wise features
                   II. R ELATED W ORK                           of PointNet [12] instead of point histograms for extent
                                                                regression. While these methods work well for indoor
Hand Crafted Features For Proposal Generation: Before
                                                                scenes and brightly lit outdoor scenes, they are expected
the emergence of 3D Region Proposal Networks (RPNs)
                                                                to perform poorly in more extreme outdoor scenarios. Any
[2], 3D proposal generation algorithms typically used hand-
                                                                missed 2D detections will lead to missed 3D detections
crafted features to generate a small set of candidate boxes
                                                                and therefore, the generalization capability of such methods
that retrieve most of the objects in 3D space. 3DOP [6]
                                                                under such extreme conditions has yet to be demonstrated.
and Mono3D [7] uses a variety of hand-crafted geometric
                                                                LIDAR data is much less variable than image data and we
features from stereo point clouds and monocular images
                                                                show in Section IV that AVOD is robust to noisy LIDAR
to score 3D sliding windows in an energy minimization
                                                                data and lighting changes, as it was tested in snowy scenes
framework. The top K scoring windows are selected as
                                                                and in low light conditions.
region proposals, which are then consumed by a modified
Fast-RCNN [?] to generate the final 3D detections. We use
a region proposal network that learns features from both        Monocular-Based 3D Object Detectors: Another way
BEV and image spaces to generate higher quality proposals       to utilize mature 2D object detectors is to use prior
in an efficient manner.                                         knowledge to perform 3D object detection from monocular
                                                                images only. Deep MANTA [13] proposes a many-task
Proposal Free Single Shot Detectors: Single shot object         vehicle analysis approach from monocular images that
detectors have also been proposed as RPN free architectures     optimizes region proposal, detection, 2D box regression,
for the 3D object detection task. VeloFCN [3] projects a        part localization, part visibility, and 3D template prediction
LIDAR point cloud to the front view, which is used as an        simultaneously. The architecture requires a database of 3D
input to a fully convolutional neural network to directly       models corresponding to several types of vehicles, making
generate dense 3D bounding boxes. 3D-FCN [8] extends            the proposed approach hard to generalize to classes where
this concept by applying 3D convolutions on 3D voxel grids      such models do not exist. Deep3DBox [14] proposes to
constructed from LIDAR point clouds to generate better 3D       extend 2D object detectors to 3D by exploiting the fact that
bounding boxes. Our two-stage architecture uses an RPN to       the perspective projection of a 3D bounding box should fit
retrieve most object instances in the road scene, providing     tightly within its 2D detection window. However, in Section
IV, these methods are shown to perform poorly on the 3D         therefore takes as an input an M × N × D image or BEV
                                                                                                     ∗
detection task compared to methods that use point cloud data.   map, and produces an M        N
                                                                                         8 × 8 × D feature map F . F has
                                                                high representational power, but is 8× lower in resolution
3D Region Proposal Networks: 3D RPNs have previously            compared to the input. An average pedestrian in the KITTI
been proposed in [15] for 3D object detection from RGBD         dataset occupies 0.8 × 0.6 meters in the BEV. This translates
images. However, up to our knowledge, MV3D [4] is the           to an 8×6 pixel area in a BEV map with 0.1 meter resolution.
only architecture that proposed a 3D RPN targeted at au-        Downsampling by 8× results in these small classes to occupy
tonomous driving scenarios. MV3D extends the image based        less than one pixel in the output feature map, that is without
RPN of Faster R-CNN [2] to 3D by corresponding every            taking into account the increase in receptive field caused
pixel in the BEV feature map to multiple prior 3D anchors.      by convolutions. Inspired by the Feature Pyramid Network
These anchors are then fed to the RPN to generate 3D            (FPN) [5], we create a bottom-up decoder that learns to
proposals that are used to create view-specific feature crops   upsample the feature map back to the original input size,
from the BEV, front view of [3], and image view feature         while maintaining run time speed. The decoder takes as an
maps. A deep fusion scheme is used to combine information       input the output of the encoder, F , and produces a new
from these feature crops to produce the final detection         M × N × D̃ feature map. Fig. 3 shows the operations
output. However, this RPN architecture does not work well       performed by the decoder, which include upsampling of
for small object instances in BEV. When downsampled by          the input via a conv-transpose operation, concatenation of
convolutional feature extractors, small instances will occupy   a corresponding feature map from the encoder, and finally
a fraction of a pixel in the final feature map, resulting in    fusing the two via a 3 × 3 convolution operation. The final
insufficient data to extract informative features. Our RPN      feature map is of high resolution and representational power,
architecture aims to fuse full resolution feature crops from    and is shared by both the RPN and the second stage detection
the image and the BEV feature maps as inputs to the             network.
RPN, allowing the generation of high recall proposals for
smaller classes. Furthermore, our feature extractor provides    C. Multimodal Fusion Region Proposal Network
full resolution feature maps, which are shown to greatly help      Similar to 2D two-stage detectors, the proposed RPN
in localization accuracy for small objects during the second    regresses the difference between a set of prior 3D boxes
stage of the detection framework.                               and the ground truth. These prior boxes are referred
                                                                to as anchors, and are encoded using the axis aligned
             III. T HE AVOD A RCHITECTURE                       bounding box encoding shown in Fig. 4. Anchor boxes are
   The proposed method, depicted in Fig. 2, uses feature        parameterized by the centroid (tx , ty , tz ) and axis aligned
extractors to generate feature maps from both the BEV map       dimensions (dx , dy , dz ). To generate the 3D anchor grid,
and the RGB image. Both feature maps are then used by the       (tx , ty ) pairs are sampled at an interval of 0.5 meters in
RPN to generate non-oriented region proposals, which are        BEV, while tz is determined based on the sensor’s height
passed to the detection network for dimension refinement,       above the ground plane. The dimensions of the anchors are
orientation estimation, and category classification.            determined by clustering the training samples for each class.
                                                                Anchors without 3D points in BEV are removed efficiently
A. Generating Feature Maps from Point Clouds and Images         via integral images resulting in 80 − 100K non-empty
   We follow the procedure described in [4] to generate a       anchors per frame.
six-channel BEV map from a voxel grid representation of
the point cloud at 0.1 meter resolution. The point cloud        Extracting Feature Crops Via Multiview Crop And
is cropped at [−40, 40] × [0, 70] meters to contain points      Resize Operations: To extract feature crops for every
within the field of view of the camera. The first 5 channels    anchor from the view specific feature maps, we use the
of the BEV map are encoded with the maximum height              crop and resize operation [17]. Given an anchor in 3D, two
of points in each grid cell, generated from 5 equal slices      regions of interest are obtained by projecting the anchor
between [0, 2.5] meters along the Z axis. The sixth BEV         onto the BEV and image feature maps. The corresponding
channel contains point density information computed per cell    regions are then used to extract feature map crops from
as min(1.0, log(N  +1)
               log 16 ), where N is the number of points in
                                                                each view, which are then bilinearly resized to 3 × 3 to
the cell.                                                       obtain equal-length feature vectors. This extraction method
                                                                results in feature crops that abide by the aspect ratio of the
B. The Feature Extractor                                        projected anchor in both views, providing a more reliable
   The proposed architecture uses two identical feature ex-     feature crop than the 3 × 3 convolution used originally by
tractor architectures, one for each input view. The full-       Faster-RCNN.
resolution feature extractor is shown in Fig. 3 and is com-
prised of two segments: an encoder and a decoder. The           Dimensionality Reduction Via 1 × 1 Convolutional
encoder is modeled after VGG-16 [16] with some modifica-        Layers: In some scenarios, the region proposal network is
tions, mainly a reduction of the number of channels by half,    required to save feature crops for 100K anchors in GPU
and cutting the network at the conv-4 layer. The encoder        memory. Attempting to extract feature crops directly from
high dimensional feature maps imposes a large memory
overhead per input view. As an example, extracting 7 × 7
feature crops for 100K anchors from a 256-dimensional
feature map requires around 5 gigabytes1 of memory
assuming 32-bit floating point representation. Furthermore,
processing such high-dimensional feature crops with the
RPN greatly increases its computational requirements.
   Inspired by their use in [18], we propose to apply a 1 × 1
convolutional kernel on the output feature maps from each
view, as an efficient dimensionality reduction mechanism
that learns to select features that contribute greatly to the
performance of the region proposal generation. This reduces
the memory overhead for computing anchor specific feature
crops by D̃×, allowing the RPN to process fused features
of tens of thousands of anchors using only a few megabytes       Fig. 3: The architecture of our proposed high resolution
of additional memory.                                            feature extractor shown here for the image branch. Feature
                                                                 maps are propagated from the encoder to the decoder section
3D Proposal Generation: The outputs of the crop                  via red arrows. Fusion is then performed at every stage of
and resize operation are equal-sized feature crops from both     the decoder by a learned upsampling layer, followed by
views, which are fused via an element-wise mean operation.       concatenation, and then mixing via a convolutional layer,
Two task specific branches [2] of fully connected layers of      resulting in a full resolution feature map at the last layer of
size 256 use the fused feature crops to regress axis aligned     the decoder.
object proposal boxes and output an object/background
“objectness” score. 3D box regression is performed by            corner and height offsets from the ground plane between
computing (∆tx , ∆ty , ∆tz , ∆dx , ∆dy , ∆dz ), the difference   the proposals and the ground truth boxes. To determine
in centroid and dimensions between anchors and ground            corner offsets, we correspond the closest corner of the
truth bounding boxes. Smooth L1 loss is used for 3D box          proposals to the closest corner of the ground truth box in
regression, and cross-entropy loss for “objectness”. Similar     BEV. The proposed encoding reduces the box representation
to [2], background anchors are ignored when computing            from an overparameterized 24 dimensional vector to a 10
the regression loss. Background anchors are determined           dimensional one.
by calculating the 2D IoU in BEV between the anchors
and the ground truth bounding boxes. For the car class,          Explicit Orientation Vector Regression: To determine
anchors with IoU less than 0.3 are considered background         orientation from a 3D bounding box, MV3D [4] relies on the
anchors, while ones with IoU greater than 0.5 are considered     extents of the estimated bounding box where the orientation
object anchors. For the pedestrian and cyclist classes, the      vector is assumed to be in the direction of the longer side of
object anchor IoU threshold is reduced to 0.45. To remove        the box. This approach suffers from two problems. First, this
redundant proposals, 2D non-maximum suppression (NMS)            method fails for detected objects that do not always obey
at an IoU threshold of 0.8 in BEV is used to keep the            the rule proposed above, such as pedestrians. Secondly, the
top 1024 proposals during training. At inference time, 300       resulting orientation is only known up to an additive constant
proposals are used for the car class, whereas 1024 proposals     of ±π radians. Orientation information is lost as the corner
are kept for pedestrians and cyclists.                           order is not preserved in closest corner to corner matching.
                                                                 Fig. 1 presents an example of how the same rectangular
D. Second Stage Detection Network                                bounding box can contain two instances of an object with
3D Bounding Box Encoding: In [4], Chen et al. claim              opposite orientation vectors. Our architecture remedies
that 8 corner box encoding provides better results than the      this problem by computing (xθ , yθ ) = (cos(θ), sin(θ)).
traditional axis aligned encoding previously proposed in         This orientation vector representation implicitly handles
[15]. However, an 8 corner encoding does not take into           angle wrapping as every θ ∈ [−π, π] can be represented
account the physical constraints of a 3D bounding box, as        by a unique unit vector in the BEV space. We use the
the top corners of the bounding box are forced to align with     regressed orientation vector to resolve the ambiguity in the
those at the bottom. To reduce redundancy and keep these         bounding box orientation estimate from the adopted four
physical constraints, we propose to encode the bounding          corner representation, as this experimentally found to be
box with four corners and two height values representing         more accurate than using the regressed orientation directly.
the top and bottom corner offsets from the ground plane,         Specifically, we extract the four possible orientations of
determined from the sensor height. Our regression targets        the bounding box, and then choose the one closest to the
are therefore (∆x1 ...∆x4 , ∆y1 ...∆y4 , ∆h1 , ∆h2 ), the        explicitly regressed orientation vector.

  1 100, 000 × 7 × 7 × 256 × 4 bytes.                            Generating    Final    Detections: Similar to the RPN,
                                                                 We compare three variants of our RPN against the proposal
                                                                 generation algorithms 3DOP [6] and Mono3D [7]. Fig. 5
                                                                 shows the recall vs number of proposals curves for our
                                                                 RPN variants, 3DOP and Mono3D. It can be seen that
                                                                 our RPN variants outperform both 3DOP and Mono3D by
                                                                 a wide margin on all three classes. As an example, our
                                                                 Feature Pyramid based fusion RPN achieves an 86% 3D
                                                                 recall on the car class with just 10 proposals per frame.
Fig. 4: A visual comparison between the 8 corner box             The maximum recall achieved by 3DOP and Mono3D on
encoding proposed in [4], the axis aligned box encoding          the car class is 73.87% and 65.74% respectively. This
proposed in [15], and our 4 corner encoding.                     gap is also present for the pedestrian and cyclist classes,
                                                                 where our RPN achieves more than 20% increase in recall
the inputs to the multiview detection network are feature        at 1024 proposals. This large gap in performance suggests
crops generated from projecting the proposals into the two       the superiority of learning based approaches over methods
input views. As the number of proposals is an order of           based on hand crafted features. For the car class, our RPN
magnitude lower than the number of anchors, the original         variants achieve a 91% recall at just 50 proposals, whereas
feature map with a depth of D̃ = 32 is used for generating       MV3D [4] reported requiring 300 proposals to achieve the
these feature crops. Crops from both input views are resized     same recall. It should be noted that MV3D does not publicly
to 7×7 and then fused with an element-wise mean operation.       provide proposal results for cars, and was not tested on
A single set of three fully connected layers of size 2048        pedestrians or cyclists.
process the fused feature crops to output box regression,
orientation estimation, and category classification for each     3D Object Detection 3D detection results are evaluated
proposal. Similar to the RPN, we employ a multi-task loss        using the 3D and BEV AP and Average Heading Similarity
combining two Smooth L1 losses for the bounding box and          (AHS) at 0.7 IoU threshold for the car class, and 0.5
orientation vector regression tasks, and a cross-entropy loss    IoU threshold for the pedestrian and cyclist classes. The
for the classification task. Proposals are only considered in    AHS is the Average Orientation Similarity (AOS) [1],
the evaluation of the regression loss if they have at least a    but evaluated using 3D IOU and global orientation angle
0.65 or 0.55 2D IoU in BEV with the ground truth boxes           instead of 2D IOU and observation angle, removing the
for the car and pedestrian/cyclist classes, respectively. To     metric’s dependence on localization accuracy. We compare
remove overlapping detections, NMS is used at a threshold        against publicly provided detections from MV3D [4] and
of 0.01.                                                         Deep3DBox [14] on the validation set. It has to be noted
                                                                 that no currently published method publicly provides
E. Training
                                                                 results on the pedestrian and cyclist classes for the 3D
   We train two networks, one for the car class and one          object detection task, and hence comparison is done for
for both the pedestrian and cyclist classes. The RPN and         the car class only. On the validation set (Table I), our
the detection networks are trained jointly in an end-to-end      architecture is shown to outperform MV3D by 2.09% AP
fashion using mini-batches containing one image with 512         on the moderate setting and 4.09% on the hard setting.
and 1024 ROIs, respectively. The network is trained for          However, AVOD achieves a 30.36% and 28.42% increase
120K iterations using an ADAM optimizer with an initial          in AHS over MV3D at the moderate and hard setting
learning rate of 0.0001 that is decayed exponentially every      respectively. This can be attributed to the loss of orientation
30K iterations with a decay factor of 0.8.                       vector direction discussed in Section III-D resulting in
                                                                 orientation estimation up to an additive error of ±π radians.
              IV. E XPERIMENTS AND R ESULTS
                                                                 To verify this assertion, Fig. 7 shows a visualization of
   We test AVOD’s performance on the proposal generation         the results of AVOD and MV3D in comparison to KITTI’s
and object detection tasks on the three classes of the KITTI     ground truth. It can be seen that MV3D assigns erroneous
Object Detection Benchmark [1]. We follow [4] to split           orientations for almost half of the cars shown. On the
the provided 7481 training frames into a training and a          other hand, our proposed architecture assigns the correct
validation set at approximately a 1 : 1 ratio. For evaluation,
we follow the easy, medium, hard difficulty classification
                                                                                                  Easy         Moderate            Hard
proposed by KITTI. We evaluate and compare two versions
                                                                                             AP      AHS      AP     AHS      AP      AHS
of our implementation, Ours with a VGG-like feature
                                                                        Deep3DBox            5.84     5.84    4.09    4.09    3.83     3.83
extractor similar to [4], and Ours (Feature Pyramid) with                  MV3D             83.87    52.74   72.35   43.75   64.56    39.86
the proposed high resolution feature extractor described in        Ours (Feature Pyramid)   84.41    84.19   74.44   74.11   68.65    68.28
Section III-B.
                                                                 TABLE I: A comparison of the performance of Deep3DBox
3D Proposal Recall: 3D proposal generation is evaluated          [14], MV3D [4], and our method evaluated on the car class
using 3D bounding box recall at a 0.5 3D IoU threshold.          in the validation set. For evaluation, we show the AP and
                                                                 AHS (in %) at 0.7 3D IoU.
                      Car                                    Pedestrian                                      Cyclist
  recall

                                           recall

                                                                                         recall
                # of proposals                              # of proposals                                # of proposals

Fig. 5: Recall vs. number of proposals at a 3D IoU threshold of 0.5 for the three classes evaluated on the validation set at
moderate difficulty.
                                                                AP3D (%)                       APBEV (%)
                  Method            Runtime (s)     Class      Easy      Moderate     Hard        Easy      Moderate   Hard
                 MV3D [4]              0.36                    71.09       62.35      55.12       86.02      76.90     68.49
                VoxelNet [9]           0.23                    77.47       65.11      57.73       89.35      79.26     77.39
              F-PointNet [11]          0.17         Car        81.20       70.39      62.19       88.70      84.00     75.33
                    Ours               0.08                    73.59       65.78      58.38       86.80      85.44     77.73
           Ours (Feature Pyramid)       0.1                    81.94       71.88      66.38       88.53      83.79     77.90
                VoxelNet [9]           0.23                    39.48       33.69      31.51       46.13      40.74     38.11
              F-PointNet [11]          0.17                    51.21       44.89      40.23       58.09      50.22     47.20
                                                    Ped.
                    Ours               0.08                    38.28       31.51      26.98       42.51      35.24     33.97
           Ours (Feature Pyramid)      0.1                     50.80       42.81      40.88       58.75      51.05     47.54
                VoxelNet [9]           0.23                    61.22       48.36      44.37       66.70      54.76     50.55
              F-PointNet [11]          0.17                    71.96       56.77      50.39       75.38      61.96     54.68
                                                    Cyc.
                    Ours               0.08                    60.11       44.90      38.80       63.66      47.74     46.55
           Ours (Feature Pyramid)      0.1                     64.00       52.18      46.61       68.06      57.48     50.77

TABLE II: A comparison of the performance of AVOD with the state of the art 3D object detectors evaluated on KITTI’s
test set. Results are generated by KITTI’s evaluation server [19].

orientation for all cars in the scene. As expected, the gap           slightly above F-PointNet on hard instances using 3D AP.
in 3D localization performance between Deep3DBox and                  On the cyclist class, our method falls short to F-PointNet.
our proposed architecture is very large. It can be seen in            We believe that this is due to the low number of cyclist
Fig. 7 that Deep3DBox fails at accurately localizing most of          instances in the KITTI dataset, which induces a bias towards
the vehicles in 3D. This further enforces the superiority of          pedestrian detection in our joint pedestrian/cyclist network.
fusion based methods over monocular based ones. We also
compare the performance of our architecture on the KITTI
test set with MV3D, VoxelNet[9], and F-PointNet[11]. Test             Runtime and Memory Requirements: We use FLOP count
set results are provided directly by the evaluation server,           and number of parameters to assess the computational effi-
which does not compute the AHS metric. Table II shows                 ciency and the memory requirements of the proposed net-
the results of AVOD on KITTI’s test set. It can be seen               work. Our final Feature Pyramid fusion architecture employs
that even with only the encoder for feature extraction, our           roughly 38.073 million parameters, approximately 16% that
architecture performs quite well on all three classes, while          of MV3D. The deep fusion scheme employed by MV3D
being twice as fast as the next fastest method, F-PointNet.           triples the number of fully connected layers required for
However, once we add our high-resolution feature extractor            the second stage detection network, which explains the
(Feature Pyramid), our architecture outperforms all other             significant reduction in the number of parameters by our
methods on the car class in 3D object detection, with a               proposed architecture. Furthermore, our Feature Pyramid
noticeable margin of 4.19% on hard (highly occluded or                fusion architecture requires 231.263 billion FLOPs per frame
far) instances in comparison to the second best performing            allowing it to process frames in 0.1 seconds on a TITAN Xp
method, F-PointNet. On the pedestrian class, our Feature              GPU, taking 20ms for pre-processing and 80ms for inference.
Pyramid architecture ranks first in BEV AP, while scoring             This makes it 1.7× faster than F-PointNet, while maintaining
                                                                      state-of-the-art results. Finally, our proposed architecture
Fig. 6: Qualitative results of AVOD for cars (top) and pedestrians/cyclists (bottom). Left: 3D region proposal network output,
Middle: 3D detection output, and Right: the projection of the detection output onto image space for all three classes. The
3D LIDAR point cloud has been colorized and interpolated for better visualization.

requires only 2 gigabytes of GPU memory at inference time,
making it suitable to be used for deployment on autonomous
vehicles.

A. Ablation Studies:
   Table III shows the effect of varying different hyperpa-
rameters on the performance measured by the AP and AHS,
number of model parameters, and FLOP count of the pro-
posed architecture. The base network uses hyperparameter
values described throughout the paper up to this point, along
with the feature extractor of MV3D. We study the effect
of the RPN’s input feature vector origin and size on both
the proposal recall and final detection AP by training two
networks, one using BEV only features and the other using
feature crops of size 1×1 as input to the RPN stage. We also
study the effect of different bounding box encoding schemes
shown in Fig. 4, and the effects of adding an orientation
regression output layer on the final detection performance in
terms of AP and AHS. Finally, we study the effect of our         Fig. 7: A qualitative comparison between MV3D [4],
high-resolution feature extractor, compared to the original      Deep3DBox [14], and our architecture relative to KITTI’s
one proposed by MV3D.                                            ground truth on a sample in the validation set.

RPN Input Variations: Fig. 5 shows the recall vs number          both image and BEV features at the RPN stage results in a
of proposals curves for both the original RPN and BEV            6.9% and 9.4% increase in AP over the BEV only version
only RPN without the feature pyramid extractor on the three      for the pedestrian and cyclist classes respectively.
classes on the validation set. For the pedestrian and cyclist
classes, fusing features from both views at the RPN stage        Bounding Box Encoding: We study the effect of different
is shown to provide a 10.1% and 8.6% increase in recall          bounding box encodings shown in Fig. 4 by training two
over the BEV only version at 1024 proposals. Adding our          additional networks. The first network estimates axis aligned
high-resolution feature extractor increases this difference to   bounding boxes, using the regressed orientation vector as
around 10.5% and 10.8% for the respective classes. For the       the final box orientation. The second and the third networks
car class, adding image features as an input to the RPN, or      use our 4 corner and MV3D’s 8 corner encodings without
using the high resolution feature extractor does not seem to     additional orientation estimation as described in Section
provide a higher recall value over the BEV only version.         III-D. As expected, without orientation regression to provide
We attribute this to the fact that instances from the car        orientation angle correction, the two networks employing
class usually occupy a large space in the input BEV map,         the 4 corner and the 8 corner encodings provide a much
providing sufficient features in the corresponding output low    lower AHS than the base network for all three classes. This
resolution feature map to reliably generate object proposals.    phenomenon can be attributed to the loss of orientation
The effect of the increase in proposal recall on the final       information as described in Section III-D.
detection performance can be observed in Table III. Using
                                                          Car           Pedestrian       Cyclist
                    Architecture                   AP       AHS        AP     AHS      AP      AHS      Number Of Parameters              FLOPs

                 Base Network                      74.1         73.9   39.5   29.8    41.6     33.2            38,073,528            186,284,945,569
                RPN BEV Only                       74.1         73.9   32.6   26.7    32.2     30.2                 0                 -266,641,569
              Axis-Aligned Boxes                   67.6         67.5   36.2   27.8    36.6     35.7              -8196                      0
      Non-Ordered 4 Corners, No Orientation        67.8         43.1   37.8   17.9    41.1     18.6              -4,098                  -20,418
      Non-Ordered 8 Corners, No Orientation        66.9         34.1   37.8   18.1    40.4     21.0             +24,638                 +122,879
           Feature Pyramid Extractor               74.4         74.1   58.8   43.3    49.7     48.7           -21,391,104            +44,978,386,776

TABLE III: A comparison of the performance of different variations of hyperparameters, evaluated on the validation set at
moderate difficulty. We use a 3D IoU threshold of 0.7 for the Car class, and 0.5 for the pedestrian and cyclist classes. The
effect of variation of hyperparameters on the FLOPs and number of parameters are measured relative to the base network.

Feature Extractor: We compare the detection results of                           [5] T.-Y. Lin, P. Dollár, R. Girshick, K. He, B. Hariharan, and S. Belongie,
our feature extractor to that of the base VGG-based feature                          “Feature pyramid networks for object detection,” in Computer Vision
                                                                                     and Pattern Recognition, vol. 1, no. 2, 2017, p. 4.
extractor proposed by MV3D. For the car class, our pyramid                       [6] X. Chen, K. Kundu, Y. Zhu, A. Berneshawi, H. Ma, S. Fidler, and
feature extractor only achieves a gain of 0.3% in AP and                             R. Urtasun, “3d object proposals for accurate object class detection,”
AHS. However, the performance gains on smaller classes is                            in NIPS, 2015.
                                                                                 [7] X. Chen, K. Kundu, Z. Zhang, H. Ma, S. Fidler, and R. Urtasun,
much more substantial. Specifically, we achieve a gain of                            “Monocular 3d object detection for autonomous driving,” in Computer
19.3% and 8.1% AP on the pedestrian and cyclist classes                              Vision and Pattern Recognition, 2016.
respectively. This shows that our high-resolution feature                        [8] B. Li, “3d fully convolutional network for vehicle detection in point
                                                                                     cloud,” in IROS, 2017.
extractor is essential to achieve state-of-the-art results on                    [9] Y. Zhou and O. Tuzel, “Voxelnet: End-to-end learning for point cloud
these two classes with a minor increase in computational                             based 3d object detection,” arXiv preprint arXiv:1711.06396, 2017.
requirements.                                                                   [10] J. Lahoud and B. Ghanem, “2d-driven 3d object detection in rgb-d
                                                                                     images,” in Proceedings of the IEEE Conference on Computer Vision
                                                                                     and Pattern Recognition, 2017, pp. 4622–4630.
Qualitative Results: Fig. 6 shows the output of the                             [11] C. R. Qi, W. Liu, C. Wu, H. Su, and L. J. Guibas, “Frustum
RPN and the final detections in both 3D and im-                                      pointnets for 3d object detection from rgb-d data,” arXiv preprint
                                                                                     arXiv:1711.08488, 2017.
age space. More qualitative results including those of                          [12] C. R. Qi, H. Su, K. Mo, and L. J. Guibas, “Pointnet: Deep learning
AVOD running in snow and night scenes are provided at                                on point sets for 3d classification and segmentation,” arXiv preprint
https://youtu.be/mDaqKICiHyA.                                                        arXiv:1612.00593, 2016.
                                                                                [13] F. Chabot, M. Chaouch, J. Rabarisoa, C. Teulière, and T. Chateau,
                                                                                     “Deep manta: A coarse-to-fine many-task network for joint 2d and 3d
                          V. C ONCLUSION                                             vehicle analysis from monocular image,” in Proceedings of the IEEE
   In this work we proposed AVOD, a 3D object detector for                           Conference on Computer Vision and Pattern Recognition, 2017.
                                                                                [14] A. Mousavian, D. Anguelov, J. Flynn, and J. Kosecka, “3d bounding
autonomous driving scenarios. The proposed architecture is                           box estimation using deep learning and geometry,” in Proceedings of
differentiated from the state of the art by using a high resolu-                     the IEEE Conference on Computer Vision and Pattern Recognition,
tion feature extractor coupled with a multimodal fusion RPN                          2017.
                                                                                [15] S. Song and J. Xiao, “Deep sliding shapes for amodal 3d object
architecture, and is therefore able to produce accurate region                       detection in rgb-d images,” in Proceedings of the IEEE Conference
proposals for small classes in road scenes. Furthermore,                             on Computer Vision and Pattern Recognition, 2016, pp. 808–816.
the proposed architecture employs explicit orientation vector                   [16] K. Simonyan and A. Zisserman, “Very deep convolutional networks
                                                                                     for large-scale image recognition,” arXiv preprint arXiv:1409.1556,
regression to resolve the ambiguous orientation estimate                             2014.
inferred from a bounding box. Experiments on the KITTI                          [17] J. Huang, V. Rathod, C. Sun, M. Zhu, A. Korattikara, A. Fathi,
dataset show the superiority of our proposed architecture                            I. Fischer, Z. Wojna, Y. Song, S. Guadarrama, and K. Murphy,
                                                                                     “Speed/accuracy trade-offs for modern convolutional object detectors,”
over the state of the art on the 3D localization, orientation                        in The IEEE Conference on Computer Vision and Pattern Recognition
estimation, and category classification tasks. Finally, the                          (CVPR), July 2017.
proposed architecture is shown to run in real time and with                     [18] F. N. Iandola, S. Han, M. W. Moskewicz, K. Ashraf, W. J. Dally,
                                                                                     and K. Keutzer, “Squeezenet: Alexnet-level accuracy with 50x fewer
a low memory overhead.                                                               parameters and¡ 0.5 mb model size,” arXiv preprint arXiv:1602.07360,
                                                                                     2016.
                            R EFERENCES                                         [19] “Kitti 3d object detection benchmark,” http://www.cvlibs.net/datasets/
                                                                                     kitti/eval object.php?obj benchmark=3d, accessed: 2018-02-28.
 [1] A. Geiger, P. Lenz, and R. Urtasun, “Are we ready for autonomous
     driving? the kitti vision benchmark suite,” in Computer Vision and
     Pattern Recognition (CVPR), 2012 IEEE Conference on. IEEE, 2012,
     pp. 3354–3361.
 [2] S. Ren, K. He, R. Girshick, and J. Sun, “Faster r-cnn: Towards real-
     time object detection with region proposal networks,” in Advances in
     Neural Information Processing Systems 28, 2015, pp. 91–99.
 [3] B. Li, T. Zhang, and T. Xia, “Vehicle detection from 3d lidar using
     fully convolutional network,” in Proceedings of Robotics: Science and
     Systems, AnnArbor, Michigan, June 2016.
 [4] X. Chen, H. Ma, J. Wan, B. Li, and T. Xia, “Multi-view 3d object
     detection network for autonomous driving,” in Computer Vision and
     Pattern Recognition, 2017. CVPR 2017. IEEE Conference on,.
